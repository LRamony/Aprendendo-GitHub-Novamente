#Crie um novo repositório na linha de comando:
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/LRamony/Aprendendo-GitHub-Novamente.git
git push -u origin main

#Ou envie um repositório existente a partir da linha de comando:

git remote add origin https://github.com/LRamony/Aprendendo-GitHub-Novamente.git
git branch -M main
git push -u origin main


-----------------------------------------------Coso seja iniciante------------------------------------------
#git init < serve para iniciar o github
#git status < serve para ver os status do seu git
#git add <Nome d arquivos> < Para adicionar o arquivo ao git
#git add . < Permite subir varios arquivos ao mesmo tempo
#git commit -m "" < Sempre tem q rodar esse comando para comentar e subir seu primeiro codigo
#Depois desses passos ele pode lhe pedir pra configurar seu login e senha do git

#git branch < mostra em qual branch vc esta trabalhando
#git checkout <Nome da branch> < Muda para a branch desejada
#git checkout -b <Nome da branch> < Cria e seta para a nova branch
#git reflog < serve para ver o historico das atualizações
#git reset --hard <ID pego com o reflog> < Como navegar entre versões do seu código
#git merge <Nome na branch> < Como unir as branch ou código
#git pull < serve para atualizar o codigo na sua maquina com o codigo no github
#touch .gitignore < So salvar dentro desse arquivo o q deseja q seja ignorado

#Antes de instalar o ambiente virtual faça isso:
https://answers.microsoft.com/pt-br/windows/forum/all/permitir-a-execu%C3%A7%C3%A3o-de-scripts-no/f6b195cf-0be7-46e2-b88c-358c79f78343#:~:text=Primeiramente%20va%20em%20pesquisar%20no,digite%20%22S%22%20para%20confirmar.

#Com instalar o virtualenv para projetos python
python -m pip install virtualenv

#Como criar um ambiente virtual (win+r)
appdata\local\programs\python\Scripts\virtualenv.exe <Nome do seu projeto>\Scritps\activate
